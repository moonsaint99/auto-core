import obspy as op

# The importer reads in SAC data representing differnet stations and events
# Inside the folder 'datasets', you can add a folder containing your dataset
# Each dataset folder contains a number of folders corresponding to all events represented in the dataset
# Each event folder will contain data from all stations that recorded the event

